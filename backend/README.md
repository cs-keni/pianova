# Pianova backend

The FastAPI backend owns configuration, persistence, project-scoped storage, and future processing stages. Use Python 3.11; the transcription extra is intentionally separate from ordinary development dependencies.
